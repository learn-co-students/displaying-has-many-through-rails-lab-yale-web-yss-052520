<h1><%= @patient.name %> Appointments: </h1>

<ul>
<% @patient.appointments.each do |a| %>
    <li>
        <%= link_to a.doctor.name, doctor_path(a.doctor) %>
        Time: <%= a.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>
    </li>
<% end %>
</ul>
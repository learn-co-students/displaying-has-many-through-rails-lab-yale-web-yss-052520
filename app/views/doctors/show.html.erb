<h1><%= @doctor.name%></h1>
<h2><%= @doctor.department %></h2>
<ul>
    <% @doctor.appointments.each do |a| %>
        <li><%= a.formatted_time %><%= link_to a.patient.name, patient_path(a.patient)  %></li>
    <% end %>
</ul>